# AlfaRev Demo — Deploy

Static build deployed to GitHub Pages.

**URL:** https://nd-fe-b.github.io/sandfire-demo/
**Password:** Perth

This directory has its own `.git` — it's a separate repo for GitHub Pages deployment. The parent repo tracks it as a submodule pointer.

**Source:** `../wireframe_asset_analyst.html` (single-file app, built into `index.html` + `assets/`)
